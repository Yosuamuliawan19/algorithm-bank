#include<iostream>
#include<stdio.h>
#include<string>
#include<cstring>
using namespace std;
int main(int argc, char const *argv[])
{
	cin >> a;
	int arr[25][25] = {}
	for (int i = 0; i < a; ++i)
	{
		arr[25][i] = i;
	}

	while(true){
		string x;
		getline(cin,x);
		if (x == "quit"){
			break;
		}

		if (x.substr(0,4) == "move"){
			if (x.)
		}else{

		}
	}
	return 0;
}