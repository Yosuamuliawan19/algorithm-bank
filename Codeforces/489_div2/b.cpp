#include<iostream>
#include<vector>
#include<stack>
#include<algorithm>
#include<map>

typedef long long ll;
using namespace std;
ll gcd(ll a, ll b){
     if (a == 0) return b;
     return gcd(b, a%b);
}
int main (){
     ll l,r,x,y;
     cin >> l >> r >> x >> y;
     for (ll i=x;i<=y;i++){
          

     }
     return 0;
}
