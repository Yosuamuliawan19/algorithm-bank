#include<iostream>
#include<stdio.h>
#include<string>
#include<cstring>
#include<algorithm>
#include<cmath>
#include<cctype>
#include<iomanip>
#include<vector>
#define LL  long long
#define FOR(i,a,b) for(int i=a;i<=b;i++)
using namespace std;
int main(int argc, char const *argv[])
{
	int n , m , k;
	cin >> n >> m >> k ;
	for (int i = 0; i < k; ++i)
	{
		int temp;
		cin >> temp;
	}
	int total = 1;
	for (int i = 0; i < n - k; ++i)
	{
		total *= m - 1;
	}
	total / 
	return 0;
}