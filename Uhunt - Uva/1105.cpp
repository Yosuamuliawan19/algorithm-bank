#include<bits/stdc++.h>
using namespace std;
typedef long long ll;
int main (){
     ios_base::sync_with_stdio(false);
     cin.tie(NULL);
     cout.tie(NULL);
     ll x,y,n,q;
     while (cin >> x >> y >> n >> q){
          if (x == y == n == q == 0) break;
          for (int i=0;i<n;i++){
               cin >> a >> b;
          }
          for (int i=0;i<q;i++){
               cin >> m;
          }
     }



     return 0;
}
